export type data = {
    id: number,
    icon: string,
    name: string,
    created: string,
    category: string,
    content: string,
    Dates: string
}

export type archiveMenu =
    {
        id: string;
        icon: string;
        name: string;
        active: number;
        archive: number;
    }

